# Notification Sounds

Place the admin notification sound here:

```txt
public/sounds/notification.wav
```

Recommended format:

- `notification.mp3` for broad browser support.
- Keep it short, around `0.3s - 1.2s`.
- Small file size is best, ideally under `100KB`.
